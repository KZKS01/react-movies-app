const Form = (porps)=> {
    return(
        <div>
            <form>
            <input type="text" />
            <input type="submit" value="Search"/>
            </form>
        </div>
    )
};

export default Form;